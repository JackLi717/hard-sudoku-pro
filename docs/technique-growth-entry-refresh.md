# 成长入口与本局足迹：视觉推广

2026-09-04。基于已提交的 `ba5b9df`，沿用用户认可的技巧总览与详情设计。本轮不提交或推送，先体验效果。

## 后续足迹交互调整（基于 abef8f6）

用户第（2）点是连续向上滚动浏览全部记录的要求，不是空白滚动缺陷。

- 按最新反馈，四类筛选改成主展示卡片下方、记录列表上方的四个直接切换标签页。首次进入足迹页默认选中「学习」；复盘返回时仍保留用户当前选择。按用户确认，中文采用「全部／学习／用过／可能用过」，英文为「All／Learning／Used／Maybe used」，日文为「すべて／学び／使った／使ったかも」，德文为「Alle／Lernen／Genutzt／Vielleicht genutzt」。学习包含查看、使用提示和跟随讲解；可能用过保留电脑分析出的可能解法、相关收尾和无法确定的记录，保留不确定的含义。此次仅调整名称，不改变分组规则。标签宽度随文字分配，空间不足时保持单行横向滚动，不折行、不压缩文字，不使用下拉框。
- 对局与活动时间显示相对时间：刚刚、分钟前、小时前、天前，满7天显示1周前，满8天使用本地日期。页面可见时每分钟刷新，返回页面或回到前台时立即刷新；未知时间不造日期。
- 每条记录以及主展示卡片直接显示复盘步骤，例如「第37/70步」或「第37–40/70步」。一次只读加载整局回放，使用与复盘页相同的事件/候选更新帧序号和总数；引用缺失时不猜测步数，不重新分析或写入记录。
- 主展示技巧选取规则保持现状：首页取最近记录；本局优先应用、其次学习、最后可能解释，同类取最近记录，最后按稳定ID打破平局。目前没有难度、关键作用或代表性评分。
- 全部匹配记录交给同一个虚拟化列表连续滚动，不再使用「查看更多」分页按钮，末尾显示结束标记。返回复盘后保留原列表与筛选。
- 点击记录直接调用原稳定引用回看过程；取消记录展开、预览选择和技巧介绍入口。每行只另外保留来源按钮；进行中对局仍禁止回看。
- 来源弹窗用两列显示技巧、记录类型、实际活动时间、源对局时间、难度，以及存在时的候选技巧。缺失时间标为未记录，不显示归因解释段落。此变化仅用于足迹页，技巧总览和详情保持原交互。

本次相关45项测试通过，覆盖四语言标签切换、时间边界与自动刷新、单步/多步范围及回放总数、缺失引用及未保存步骤的标注、单次只读加载、连续完整数据、直接深链、来源事实表格与复盘返回状态。TypeScript、修改文件Lint与格式检查通过。本次 Android 只读截图保存在 `.local/behavior-regression/footprint-tabs-20260904/android-tabs.png` 和 `android-step-ranges.png`，确认单行四标签与真实单步/多步位置显示；未接管正在使用设备的用户导航。

前一次下拉框版本的 Android 截图在 `.local/behavior-regression/footprint-toolbar-20260904/android-list.png`，仅用于保留历史验证，不代表本次标签页版本。设备同时被用户操作，避免接管其导航。

以下为此前 abef8f6 改版及验收记录。

## 展示调整

- 首页：紧凑的技巧图形、标题、最近一条真实记录的技巧名和来源短标签。入口仍在开始/继续、历史对局之后，不使用分数、进度环或奖杯。
- 完成页：突出本局一条有依据的经历和「查看全部足迹」。展示优先级为已有应用记录、已有学习记录、可能解释，同类取活动时间最近的一条；这是展示选取，不改变归因准入和计数。下一局、选择难度、完整复盘仍保留原位置和行为。
- 本局足迹：一条真实记录的操作前棋盘与回看入口；全部记录以单列可展开列表展示。学习接触、应用记录、值得回看筛选保留，列表分批加载，未知与相关收尾也可访问。
- 提示显示「当时查看提示」或「提示辅助」，可能解释直接标注。归因理由、替代解释、实际活动时间和源对局时间按需展开。无记录、整理中、证据缺失、失败分别呈现；无预览时不造棋盘，完整复盘仍可访问。

足迹进入技巧详情或原复盘时保持挂载，保留筛选、分页、展开项和原滚动位置。重新整理数据不会强制滚动；明确点击「预览这条记录」才回到顶部。预览仅调用已有只读回放源，稳定引用改变才重读，并忽略过期异步响应。进行中对局仍不开放技巧复盘。

## 保持的边界

应用层、投影器、成长统计、事件序列化、数据库、提示/复盘分析、归因准入、奖励和轻反馈实现均未修改。首页/完成页仍通过原 `GrowthSummary` 接口接入，设置开关沿用原路由判断。已认可的总览/详情布局不变，未新增动画、课程、依赖或持久化版本。

新增展示主要文字保持16–32逻辑单位，按钮至少48，展开行至少64。记录名自动换行，大字时棋盘与操作纵向排列；装饰图形不进入读屏焦点，真实棋盘提供步骤说明，按钮提供选中/展开语义。未宣称完成实体设备的 VoiceOver/TalkBack 全流程。

## 验收方法与产物

目录：`.local/behavior-regression/growth-entry-refresh-20260904/`。

原 Android 与 iPad Mini 保存了首页、足迹页改版前画面。期间原模拟器仍被用户使用，因此额外创建独立 iPad Mini 验收实例，并启动只读 Pixel 4a Android 实例，避免打断原设备或修改其数据。

独立实例使用同一份从 iPad 用户数据库只读导出的10局原始投影、状态、move和event，通过现有反序列化与回放适配器呈现。Saved 是原记录；Hint/Possible 是筛选出的真实记录；Empty/Pending/Failed 是明确构造的展示状态。完成页使用原完成对局快照，不执行完成命令、不发奖励。截图顶端的 QA 栏属于临时验收入口，正式应用没有此栏。

`*-before-*-saved.png` 与 `*-after-*-saved.png` 在相同设备、数据与语言下比较；旧组件来自 `ba5b9df`。`*-after-footprint-hint/possible/empty/pending/failed.png` 为明确标注的边界状态样例。设备截图用于视觉检查，返回位置与异步更新同时由组件交互测试覆盖。

临时入口、旧组件副本与导出数据在验收后移出产品源码，仅保存于忽略的本地产物目录。正式体验入口：

- 首页 → 我的技巧。
- 历史对局 → 任一对局的本局技巧足迹。
- 正常完成一局 → 本局值得回看 → 查看全部足迹（遵循完成页摘要设置）。

## 检查结果

全量 Jest：52套件、945项通过，既有1套件/15项跳过。新增8项测试覆盖摘要选取与数据不变、仅提示/可能解释/空态/整理中、完整分页和筛选、展开与返回位置、异步更新与失败重试；原稳定复盘、设置与轻反馈相关测试通过。TypeScript、全仓库 Lint、修改文件的 Prettier 与 `git diff --check` 均通过。

Android 独立实例实测「展开依据 → 回看这个过程 → 原复盘第50/58步 R9C1填7 → 返回」，依据仍展开且按钮位置一致，结果见 `android-replay-return.json`。iPad Mini 的 `accessibility-large` 与 Android 1.5倍字体均检查三处新增展示；棋盘与操作纵向回退，文字换行、入口可访问。验收后独立实例恢复标准字号，原设备未被用于构造状态或注入数据。独立验收入口没有数据库连接或写入能力；不将用户同时使用原设备造成的变化称为“数据完全不变”。

本轮只涉及展示，无原生或归因算法修改，不将既有原生审计计为本轮新增算法验证。
