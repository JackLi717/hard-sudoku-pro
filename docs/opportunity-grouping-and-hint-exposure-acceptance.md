# 技巧机会归并与持久化提示曝光验收（2026-09-03）

按产品负责人要求完成原延期的两项能力，然后继续测试算法与单局技巧回顾。语义只引用[玩家技巧归因协议](player-technique-attribution-policy.md)，自动验收方式遵循[自动化验收说明](automated-behavior-acceptance.md)。

## 实现范围

- 新增共享应用层 `buildTechniqueOpportunityGroups`。保留每条动作片段和默认解释，输出机会关联、歧义/缺失/失效状态及至多一条代表记录。单局回顾读取同一投影并显示共享机会编号，不另写界面去重算法。
- 原生桥序列化 C++ 已有的完整匹配机会结果。不扩展技巧种类，不更改搜索、人力成本或最低成本选择。
- 游戏状态新增 `hintExposures`，记录接受展示的提示及其实际候选上下文。复用现有命令与钱包事务，不依赖异步诊断，不进入撤销快照。未应用的提示不会因恢复曝光而删除逻辑候选。
- 曝光缺失不能当成未使用提示；历史完整性未知时归因拒绝放行。当前开发 schema 原位扩展，不增加协议版本或数据库迁移，不重置现有对局。
- 不写正式成长档案，不产生掌握度、成长分数或发现时间结论。`growthReleaseReady=false`。

## 自动复验

执行 `node tools/behavior-evaluation/run_acceptance.mjs`，13 个阶段全部通过，`engineeringPassed=true`。报告目录：

`/var/folders/5g/pxgvwb694mjczcphtcb7yq5w0000gn/T/behavior-acceptance-lE5LoG`

- 全量 Jest：35 个套件、481 项通过；1 项依赖原生可执行文件的用例在普通 Jest 阶段跳过，随后在强制原生归并阶段执行并通过，不以跳过代替验收。
- 机会归并阶段：11 项通过，包括真实 C++ 显性数对分次删除、相同名字不同机会、相同结果不同默认技巧、歧义不传递合并、恢复观察器、提示/撤销失效和一步 placement 闭包。
- 曝光阶段：6 项通过，包括关闭提示、撤销应用、保留应用后三种 SQLite 复开；独立 Node 进程读取曝光；保存失败时提示状态/曝光/钱包一起回滚；历史缺失与损坏证据处理。
- 回顾组件验证同一机会显示同一编号，同时保留两条原始动作入口。
- TypeScript、ESLint、Prettier、`git diff --check` 通过。
- `bash scripts/test-hint-core.sh` 通过：100 道题、6166 个逻辑步骤、1000 个确定性随机合法状态，以及全部 39 项技巧的正例、反例与安全结果测试。
- 安卓 arm64 调试构建 `./gradlew :app:assembleDebug -PreactNativeArchitectures=arm64-v8a` 通过。

## 设备和下一轮测试

安卓模拟器检查时仍有一局暂停中的游戏（revision 8、提示 0），本轮没有覆盖安装、重启应用或改写设备数据库。只读副本用于确认状态，不用于回填诊断。iPad mini 本轮未更新。

需要从主仓库重新运行 `npm run android -- --active-arch-only` 更新原生桥和 JS，再继续试玩。推荐新局检查共享机会编号与提示辅助标识；没有机会结果证据的旧诊断不自动补齐，不应把它们作为新能力失败或成功的证据。

这些工程检查不证明玩家真实技巧意图，也不签收完整成长评分。新问题继续提取成固定回归样本，不要求玩家掌握高级技巧或完成独立盲审工作表。
