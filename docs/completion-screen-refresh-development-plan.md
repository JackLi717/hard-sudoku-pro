# 完成页庆祝体验改进：产品规格与开发计划

日期：2026-09-11

状态：实施中；任务 1–7 已完成，任务 8–9 待实施

## 1. 背景

当前完成页能够展示难度、用时、错误、智能提示和部分 Premium 首次完成奖励，也能继续同难度题目、
进入复盘或离开结果页。但页面主要承担结算和导航功能，完成反馈只有绿色勾号与固定标题，普通完成时
还使用“进度和奖励已安全保存”作为副标题，缺少获胜、被肯定和继续挑战的动力。

“选择其他难度”当前会先离开完成页并返回首页，再由玩家打开首页难度选择框。该路径增加一次无意义的
页面跳转，也让按钮文字与实际行为不一致。

本次改进把完成页从“结算页”调整为“胜利与下一步页面”：先明确庆祝玩家真实完成的成绩，再展示本局
数据和符合条件的道具补给领取表现，最后让玩家直接继续同难度、切换难度、复盘或返回首页。

## 2. 目标

- 每一次合法完成都有清晰、克制且有变化的庆祝反馈。
- 所有玩家都能得到视觉荣誉和与本局事实相符的表扬，不把免费用户或重玩玩家呈现为“没有奖励”。
- 继续保持 Premium 首次完成补给、库存上限和重玩不发补给等现有经济规则。
- 点击“换个难度”后直接在完成页弹出选择框；取消后仍保留当前完成页。
- 删除“进度和奖励已安全保存”及同类保存声明。
- 补齐快速铅笔使用次数和新最佳成绩；完成类型由短标题表达，补给层只表达本局应得奖励。
- 四种语言、深浅主题、动态字体、屏幕阅读器和减少动态效果保持一致体验。

## 3. 非目标与范围边界

本轮不做：

- 不新增积分、金币、经验值、每日奖励、连续签到或新的可消耗资源。
- 不改变免费、Premium、首次完成、重玩、库存上限或按难度补给的现有规则。
- 不建立可收藏奖杯、成就图鉴或新的持久化成就系统。
- 不恢复首发范围之外的“我的技巧”、本局技巧足迹或成长曲线入口。
- 不改变题目分配、难度评级、提示引擎、完成判定和失败页的产品结构。
- 不从完成页新增广告入口；广告补给继续遵守已经确定的商业化入口约束。
- 不新增第三方动画、图标或音频依赖。

若后续希望奖杯可以收藏、升级或用于解锁内容，应单独设计成就系统，不在本次视觉庆祝中暗示已经获得
持久化资产。

## 4. 产品原则

### 4.1 荣誉反馈与道具补给分层

“奖励”分为两个含义，界面必须清晰区分：

1. **荣誉反馈**：胜利勋章、庆祝动效和情境化短标题。每次完成都有，不产生库存或经济价值。
2. **本局补给**：快速铅笔和智能提示的本局应得数量。只在现有领域规则判定符合资格时一次性展示。

免费用户和重复完成不会显示“+0”或“本局没有奖励”。完成页不承担库存说明职责，不显示余额、上限、
满仓或实际入库数量；领域层继续按既有规则完成事务结算。

### 4.2 只表扬能够证明的事实

文案只使用完成状态和结算事务能够确定的事实：完美完成、独立完成、提示辅助完成、首次完成、新最佳
时间、难度和本局统计。不根据用时推断玩家“很聪明”，不根据少量数据声称能力提高，也不因使用提示或
出现失误贬低玩家。

### 4.3 庆祝与领取分工

普通完成的庆祝动画不延迟按钮出现。符合 Premium 首次完成补给资格时，结果页上方出现一次轻量领取层，
玩家点击“收下”后立即关闭；奖励在结果页出现前已经结算，“收下”只负责视觉确认，不能成为发奖事务前置
条件。领取层接入减少动态效果并允许系统返回等价关闭，避免把玩家困在弹层中。

## 5. 页面信息结构

完成页从上到下排列：

1. 奖杯或胜利勋章图形，以及短时、可降级的庆祝动效。
2. 情境化短标题和当前难度；标题自身承担鼓励与事实反馈。
3. 本局数据：用时、失误、智能提示、快速铅笔，固定为一行扁平统计条。
4. 主按钮“继续挑战难度 {{level}}”。
5. 次按钮“换个难度”，直接打开难度选择框。
6. 同一行的轻量“查看本局复盘”和“返回首页”入口。
7. 符合资格时覆盖显示一次性的 Premium 补给领取层。

补给领取层只显示标题、两个道具图示、各自 `+N` 和一个“收下”按钮，不使用资源卡片、嵌套边框、余额
或解释段落。四项统计保持一行，标签使用紧凑字号并保留逐项无障碍名称。

## 6. 完成状态与文案策略

### 6.1 主标题优先级

同一局可能同时满足多个事实。主标题按以下优先级选择，只展示最高优先级标题：

1. 新最佳时间。
2. 完美完成。
3. 首次完成该题。
4. 独立完成。
5. 提示辅助完成。

首批中文建议：

| 事实 | 主标题示例 | 鼓励语示例 |
| --- | --- | --- |
| 新最佳时间 | 新纪录！ | 比此前最快成绩又前进了一步。 |
| 完美完成 | 太漂亮了！ | 零失误、未用提示，这一局解得很干净。 |
| 首次完成 | 首次攻克！ | 你已经拿下难度 {{level}} 的一道新题。 |
| 独立完成 | 靠自己解开了！ | 有过绕路也没关系，每一步都是你推出来的。 |
| 提示辅助完成 | 稳稳拿下！ | 提示只是路标，完成整盘的是你。 |
| 通用回退 | 又解开一局！ | 复杂的盘面，被你一步步理顺了。 |

### 6.2 文案收敛

- 正式完成页不再显示轮换鼓励副标题，避免标题、标签和长句重复表达同一件事。
- 情境化短标题保持类型安全的四语资源；英语、日语、德语和简体中文均采用自然短句。
- 既有鼓励语资源暂时保留供后续实验，但不进入正式完成页渲染。

### 6.3 解题箴言

正式完成页不展示解题箴言。该内容会拉长阅读路径并削弱游戏化节奏，既有资源可保留但不参与渲染。

### 6.4 删除的文案

- 删除 `result.saved` 对应的“进度和奖励已安全保存。”及其他语言版本。
- 普通完成副标题改为情境化鼓励语，不再用保存状态占位。
- 不增加“奖励已自动领取”“数据已安全保存”等内部实现说明。

## 7. 完成事实表达

正式完成页删除荣誉标签组，由主标题表达刷新纪录、完美完成、首次完成、独立完成或提示辅助中优先级最高
的事实。若“刷新纪录”的产品口径最终选择该等级最佳而不是该题最佳，结算数据和统计页必须继续统一。

本计划推荐使用**该等级最佳时间**，更容易被玩家理解，也与“继续挑战同难度”形成连贯目标。实施前先
确认现有统计数据是否已经可靠提供该口径；若没有，不应临时改成无法说明的近似判断。

## 8. 实际补给展示

继续沿用当前规则：只有结算时具有有效 Premium 权益且首次完成该题，才获得按难度固定的快速铅笔与
智能提示补给；实际入账受 `99` 点库存上限裁剪。

领取层状态：

| 状态 | 展示 |
| --- | --- |
| Premium 首次完成 | “本局补给”，两个道具图示和按难度计算的本局应得 `+N` |
| 任一道具达到库存上限 | 展示与普通补给相同，不出现满仓或余额概念 |
| 免费用户首次完成 | 不显示领取层 |
| 重复完成 | 不显示领取层 |

进入完成页前奖励已经与完成记录在同一事务中结算。“收下”按钮只播放道具离场动画并关闭领取层，不再次
发放奖励；杀进程、重新进入或返回完成页不能导致重复结算。

## 9. 难度选择交互

### 9.1 完成页路径

点击“换个难度”后：

1. 完成页保持挂载，并在其上打开底部难度选择框。
2. 选择框复用首页的五级难度、说明和各等级完成数。
3. 点击背景、关闭按钮或系统返回时关闭选择框，仍停留在原完成页，所有文字和滚动位置保持不变。
4. 选择一个难度后关闭选择框并直接开始该难度的新题，不经过首页。
5. 题库分配、未完成题优先和全等级完成后的随机重玩继续使用现有协调器规则。
6. 启动失败时保持可恢复的结果状态并展示现有错误消息，不能留下空白页面。

本轮只改变合法完成页上的“换个难度”。失败页行为保持现状，避免把完成页批准范围扩展到失败流程。

### 9.2 组件边界

将首页内嵌的难度选择 `Modal` 提取为可复用的 `LevelPickerModal`。它只负责呈现和选择，不直接依赖
SQLite、协调器或页面路由。

建议属性：

```ts
type LevelPickerModalProps = {
  visible: boolean;
  busy: boolean;
  completedByLevel: Readonly<Record<DifficultyLevel, number>>;
  onClose(): void;
  onSelect(level: DifficultyLevel): void;
};
```

首页和完成页共同使用该组件，避免复制五级难度说明、无障碍标签和响应式样式。完成页选择难度后可调用
现有 `requestNewGame(level)`；终局会话不应触发“放弃进行中对局”的确认流程。

## 10. 结算与展示数据

当前 `GameState` 已有完成类型、用时、错误、智能提示次数和快速铅笔次数；当前完成奖励包含首次完成、
Premium 资格和实际入账数量。完成页还需要可靠的新最佳判断、奖励前后余额或等价的结算快照。

建议由完成事务返回只读的结果结算 DTO，再由 `OfflineGameSnapshot` 提供给完成页：

```ts
type CompletionResultSummary = {
  isFirstCompletion: boolean;
  isNewLevelBest: boolean;
  previousLevelBestTimeMs: number | null;
  reward: CompletionReward;
  walletBefore: Readonly<Record<CreditResource, WalletBalance>>;
  walletAfter: Readonly<Record<CreditResource, WalletBalance>>;
};
```

要求：

- 新最佳必须在更新最佳时间之前读取旧值，并在同一结算事务中产生结果，不能由 UI 事后猜测。
- 实际补给沿用库存裁剪后的数量；UI 不根据难度表重新计算应发数量。
- 余额前后值来自同一结算结果，不能从多个异步读取拼接。
- 不把鼓励语 key、动画状态或页面布局写入数据库。
- 若现有表已经能支持等级最佳查询，本轮不新增 schema；若不能，应先明确统计口径再评审数据变更。

## 11. 视觉与动效

- 将单独的绿色勾升级为奖杯或胜利勋章图形，保留数独产品的克制风格，不采用赌场式金币雨。
- 默认动效控制在约 `800–1200 ms`：奖杯轻微上移/缩放，少量星点淡入淡出。
- 页面和所有按钮立即可操作，动画不循环、不遮挡、不捕获触摸。
- 系统开启“减少动态效果”时直接显示静态终态，不仅缩短动画。
- 图形是装饰元素，不进入屏幕阅读器焦点；标题负责朗读完成结果。
- 首轮不新增声音。现有震动偏好可以在后续设备体验确认后复用，但不得绕过用户设置。
- 深色模式、四语长文案、窄屏和最大动态字体下均不得截断关键数据或操作。

## 12. 无障碍要求

- 进入完成页时，屏幕阅读器首先获得“难度 + 完成标题 + 主鼓励语”的组合信息。
- 荣誉标签逐项提供可理解的文本，不只靠颜色、星形或奖杯表达。
- 四项本局数据的朗读顺序与视觉顺序一致。
- 补给卡朗读资源名称、实际增加量和结算后可用次数；满仓时朗读明确原因。
- 难度选择框使用模态语义，打开后焦点不落到后面的完成页；关闭后焦点返回“换个难度”。
- 点击区域至少满足项目既有尺寸要求，动态字体下按钮文字允许换行。

## 13. 开发任务与执行顺序

本项目由一名开发者使用 AI Agent 完成。以下任务严格按顺序执行；每一步完成代码、定向测试和检查后，
再进入下一步。无需拆分角色、创建任务交接流程、功能分支或额外工作树。

预计主要影响：

- `src/ui/screens/ResultScreen.tsx`：完成页结构、状态呈现和操作入口。
- `src/ui/screens/HomeScreen.tsx`：移除内嵌难度弹窗，改用共用组件。
- `src/ui/components/LevelPickerModal.tsx`：新增无业务状态的共用难度选择框。
- `src/debug/CompletionResultPreview.tsx`：新增仅开发模式可见的完成页场景预览。
- `src/ui/HardSudokuApp.tsx`：完成页的开始其他难度与返回首页回调。
- `src/localization/resources.ts`：四语标题、鼓励语、标签、箴言和补给文案。
- `src/data/user/user-repository.ts` 与应用协调器：结算结果、新最佳和余额快照。
- `__tests__/`：展示模型、结果页、难度选择、协调器、仓储、本地化和无障碍覆盖。

### 任务 1：建立回归基线

状态：已完成（2026-09-11）

主要文件：`__tests__/result-screen.test.tsx`（新增）、`__tests__/offline-game-coordinator.test.ts`、
`__tests__/sqlite-data.test.ts`。

- 为当前完成页补充特征测试：完美、独立、提示辅助、免费首次完成、Premium 首次完成、重玩和满仓。
- 固定当前“继续同难度”和复盘返回完成页行为，防止视觉改版破坏终局状态。

交付：只读基线和测试夹具，不改变产品行为。

完成条件：新增测试在当前代码上通过，并能稳定构造后续任务需要的所有完成与奖励状态。

实施结果：新增完成页场景测试覆盖三种完成类型、免费首次完成、Premium 首次完成、重玩、满仓和三个
独立操作入口；新增正式单局复盘往返测试，证明返回后结算快照不变；新增 SQLite 满仓首次完成与重玩
测试，证明实际奖励、钱包、流水和统计保持现有规则。定向 5 个 suite 共 59 项测试通过，ESLint 与
TypeScript 检查通过。本任务未修改产品代码。

### 任务 2：补齐结算展示数据

状态：已完成（2026-09-11）

主要文件：`src/data/user/user-repository.ts`、`src/application/app/offline-game-coordinator.ts`、
相关导出契约、`__tests__/sqlite-data.test.ts`、`__tests__/offline-game-coordinator.test.ts`。

- 明确“刷新纪录”采用等级最佳时间口径。
- 在完成事务中读取旧最佳并返回 `CompletionResultSummary`。
- 把实际补给、结算前后余额和新最佳状态送入协调器快照。
- 覆盖首次最佳、严格更快、相同时间、较慢时间、满仓裁剪和重玩等事务测试。

交付：完成页所需事实由应用层一次性提供，UI 不推断领域结果。

完成条件：仓储和协调器定向测试通过；重复结算不重复发奖；UI 不需要额外读取 SQLite。

实施结果：新增 `CompletionResultSummary`，由完成事务一次性返回同难度旧最佳、严格刷新纪录判断、实际
入账奖励以及结算前后钱包；协调器快照直接携带该结果并在开始下一局时清空。等级最佳直接查询现有
`game_attempts` 的已完成记录，因此未修改 schema。终局结算以带版本的结构存入现有 `reward_json`，重复
提交返回首次生成的原始结算快照，同时兼容读取既有的纯奖励格式。仓储测试覆盖首次最佳、严格更快、
相同时间、较慢时间、重玩、免费、正常 Premium、满仓裁剪和幂等重复提交；协调器测试覆盖快照传递与
下一局清理。定向 5 个 suite 共 57 项测试通过，ESLint 与 TypeScript 检查通过。

### 任务 3：增加开发模式完成页预览入口

状态：已完成（2026-09-11，人工验收确认）

主要文件：`src/debug/CompletionResultPreview.tsx`（新增）、`src/ui/screens/HomeScreen.tsx`、
`src/ui/HardSudokuApp.tsx`、`__tests__/completion-result-preview.test.tsx`（新增）。

- 在首页“更多”菜单中增加一个仅 `__DEV__` 可见的“完成页预览”入口，Release 构建不显示也不可进入。
- 点击入口后先显示场景选择，再使用正式 `ResultScreen` 打开所选场景；不复制一份完成页布局。
- 提供固定且可复现的场景：免费完美首次完成、免费独立完成、提示辅助完成、Premium 正常补给、
  Premium 部分满仓、Premium 全部满仓、重复完成、新最佳和非最佳。
- 场景全部使用内存快照，不创建真实对局，不读写 SQLite，不改变钱包、统计、完成记录或奖励流水。
- 预览中的继续、换难度、复盘和返回操作不得执行真实业务命令；提供明确的“返回场景选择”和“关闭预览”。
- 使用固定场景而不是自由填写底层字段，避免产生领域上不可能的组合，也保证每次人工验收结果一致。
- 在 `emulator-5554` 上记录改版前默认、深色和 150% 字体截图；设置变化造成 Activity 重建后，从更多
  菜单重新进入同一场景即可，不再实际完成游戏。

交付：从更多菜单最多两次点击即可打开任一完成场景，能够反复进行视觉和交互验收且不污染用户数据。

完成条件：预览场景测试、Release 隐藏测试和“预览前后仓储数据不变”测试通过；三种改版前截图完成。

实施结果：在首页“更多”的开发工具区新增单一“完成页预览”入口，进入后可选择九个固定内存场景，
并由正式 `ResultScreen` 呈现结果。预览顶部始终提供“返回场景选择”和“关闭预览”；完成页内的继续、
换难度和复盘只显示预览提示，不调用协调器或仓储。新增测试覆盖九个场景事实、开发入口显示、Release
不可进入、正式组件复用、预览操作隔离，以及操作前后协调器快照、钱包、进度、统计和 SQLite 记录完全
不变。Android 开发包已安装到 `emulator-5554`，并在人工检查场景切换和视觉效果后确认提交。

### 任务 4：提取共用难度选择框

状态：已完成（2026-09-11，人工验收确认）

主要文件：`src/ui/components/LevelPickerModal.tsx`（新增）、`src/ui/screens/HomeScreen.tsx`、
`src/ui/screens/ResultScreen.tsx`、`src/ui/HardSudokuApp.tsx`、`__tests__/result-screen.test.tsx`。

- 从 `HomeScreen` 提取 `LevelPickerModal`，保持首页外观与行为不变。
- 为打开、关闭、系统返回、选择难度、忙碌禁用和无障碍标签增加组件测试。
- 在完成页接入本地开关状态和 `requestNewGame(level)`，取消后保留结果页。
- 单独验证失败页没有随共享组件重构改变行为。

交付：首页和完成页共用同一难度选择组件；完成页不再为了换难度跳回首页。

完成条件：首页行为不变；完成页能够打开、取消和选择难度；失败页行为不变；相关组件测试通过。

实施结果：将首页原有难度弹框原样提取为无业务依赖的 `LevelPickerModal`，首页与正式 `ResultScreen`
共同使用。完成状态点击“选择其他难度”直接打开弹框，选择后由协调器启动目标难度，不返回首页，也不
触发进行中对局的放弃确认；失败状态保持原有返回首页行为。任务 3 的预览仍直接渲染正式
`ResultScreen`，因此可通过同一按钮和同一 `LevelPickerModal` 验收本功能；预览选择只显示隔离提示，
不会启动对局。组件与全应用测试覆盖打开、背景取消、系统返回、选择、忙碌禁用、首页回归、失败页回归、
真实完成后直达 Level 4，以及预览前后数据不变；已在 `emulator-5554` 上完成人工交互确认。

### 任务 5：实现纯函数文案与荣誉模型

主要文件：`src/ui/screens/result-presentation.ts`（新增）、`src/localization/resources.ts`、
`__tests__/result-presentation.test.ts`（新增）和既有本地化测试。

- 建立完成展示模型，将状态映射为主标题分组、荣誉标签和箴言可见性。
- 实现基于 `sessionId` 的稳定文案选择。
- 在四种语言资源中增加对应 key，删除 `result.saved`。
- 测试事实优先级、完美标签去重、稳定轮换、未知状态回退和四语键一致性。

交付：组件只消费已经生成的展示模型，不在 JSX 中堆叠复杂条件。

完成条件：状态优先级、标签去重、稳定轮换和四语资源测试通过，正式代码不再引用 `result.saved`。

实施结果：新增纯函数 `createResultPresentation`，按“新等级最佳、完美完成、首次完成、独立完成、提示辅助、
通用回退”的固定优先级生成标题分组、鼓励语、最多三项荣誉和低频箴言；完美完成只生成“完美解答”，
不会重复生成“独立完成”。每个分组提供八条鼓励语，箴言提供八条原创短句，英语、日语、德语和简体中文
资源同步补齐；鼓励语和箴言都由 `sessionId + 文案分组` 稳定选择，箴言仅在结算后累计完成数为 4 的倍数
时进入展示模型。正式 `ResultScreen` 已直接消费该模型的标题和鼓励语，`result.saved` 已从正式代码与四语资源
删除；荣誉和箴言字段留给任务 6 的静态布局直接渲染。任务 3 的“刷新等级纪录”场景使用正式
`ResultScreen`，并固定为第 20 次累计完成，可继续用于任务 6 验收新纪录、完美荣誉与箴言组合。格式、Lint、
TypeScript 和全量 Jest 通过（78 个测试套件、3220 项测试通过，2 个套件按既有条件跳过）；已在
`emulator-5554` 的真实完成页预览确认“新纪录！”及稳定鼓励语显示，旧保存提示不再出现。

### 任务 6：重构完成页静态布局

主要文件：`src/ui/screens/ResultScreen.tsx`、`src/ui/HardSudokuApp.tsx`、
`__tests__/result-screen.test.tsx`、`__tests__/accessibility-experience.test.tsx`。

- 按第 5 节调整信息顺序，增加奖杯、荣誉标签、快速铅笔统计和条件性补给卡。
- 将主按钮改为“继续挑战难度 {{level}}”，增加“换个难度”和“返回首页”。
- 保留正式的本局复盘入口，不恢复首发范围之外的成长入口。
- 覆盖无补给、部分补给、满仓、长文案和大字体布局。

交付：静态完成页达到完整可用状态，尚不依赖动画也有清晰成就感。

完成条件：测试覆盖免费、重玩、正常 Premium、部分满仓和全部满仓；四个后续入口均符合规格。

实施结果：正式 `ResultScreen` 已改为完整静态完成页，使用胜利勋章、情境化标题与鼓励语、最多三项事实
荣誉、用时/错误/智能提示/快速铅笔四项两列统计，以及低频原创箴言。Premium 补给卡只读取任务 2 的
事务结算摘要：正常补给展示实际 `+N` 和结算后余额，部分裁剪展示实际到账、满仓项和库存上限说明，
两项均为零时只显示“库存已满，本局补给未增加”，免费首次完成和重复完成不显示补给卡。界面通过领域层
唯一的按难度补给表判断是否发生裁剪，没有复制或猜测规则。完成页提供“继续挑战难度 {{level}}”、
“换个难度”、“查看本局复盘”和较弱的“返回首页”四个入口；失败页仍保留原有重试与返回首页选择路径。
荣誉和补给在无障碍树中分别成组，长文案允许换行和系统字体缩放。任务 3 继续直接渲染正式
`ResultScreen`，所有预览操作保持隔离。格式、Lint、TypeScript 和全量 Jest 通过（78 个测试套件、
3224 项测试通过，2 个套件按既有条件跳过）；已在 `emulator-5554` 的正式完成页预览检查免费、正常
Premium 和满仓布局，静态状态下已经具备完整成就反馈。动画仍严格留在任务 7。

### 任务 7：加入克制的庆祝动效

主要文件：`src/ui/components/CompletionCelebration.tsx`（新增）、`src/ui/screens/ResultScreen.tsx`、
现有动态效果辅助模块及对应组件测试。

- 使用现有 React Native 动画能力实现一次性奖杯和星点动效。
- 接入系统“减少动态效果”，确保返回复盘时不重复播放完整庆祝。
- 检查动画期间按钮可操作、无额外渲染抖动和无屏幕阅读器噪音。

交付：默认有短庆祝，减少动态效果时为完整静态体验。

完成条件：动画只播放一次，不遮挡操作；减少动态效果和从复盘返回的行为测试通过。

实施结果：新增 `CompletionCelebration` 并由正式 `ResultScreen` 直接渲染；任务 3 的完成页预览因此自动使用
同一个正式庆祝组件，没有单独维护模拟页面。默认首次进入某个 `sessionId` 时，奖章在固定尺寸容器内完成
约 0.9 秒的轻微上移、缩放回弹和六个星点淡入淡出；动画只改变透明度与变换，不改变布局，也不接收
触摸，完成页按钮从首帧起保持可操作。播放状态保存在当前应用生命周期的页面状态容器中，从复盘或场景
列表返回同一局时直接显示终态，不重复完整庆祝；新的对局仍可播放。系统“减少动态效果”状态确认后直接
显示静态终态，读取失败时也保守地使用静态体验。整个装饰区域从无障碍树隐藏，完成标题继续承担结果朗读。
组件测试覆盖动画时长与单次播放、操作不阻塞、固定布局、装饰语义、减少动态效果和同局重新挂载；完成页
预览测试额外断言正式庆祝组件存在。格式、Lint、TypeScript 和全量 Jest 通过（79 个测试套件、3227 项
测试通过，2 个套件按既有条件跳过）；已在 `emulator-5554` 通过“更多 → 完成页预览”打开正式
`ResultScreen`，检查免费完美首次完成与免费独立完成场景的完成页终态。

### 视觉收敛改版：轻量完成页与一次性奖励领取

根据设备试玩反馈，任务 6–7 的静态信息和动效层级进行第二轮收敛：删除完成页鼓励副标题、荣誉标签、
箴言和常驻补给卡；四项统计恢复为一行扁平信息条，复盘与首页合并为轻量入口。庆祝勋章增加光环和更多
星点，使胜利反馈集中在视觉而不是文字。

Premium 首次完成使用正式 `ResultScreen` 上的一次性奖励领取层：只显示快速铅笔和智能提示图示、按难度
确定的本局应得数量及“收下”按钮。领取层不读取或展示库存、余额、满仓和裁剪状态；领域层仍在页面出现前
完成原有事务结算。点击“收下”只播放图示离场动画并关闭领取层。任务 3 的调试入口继续调用同一个正式
完成页和领取组件，正常补给、难度 5 补给及完美完成补给均可直接验收。

### 任务 8：完成自动化验收

主要范围：任务 1–7 新增或修改的全部测试，以及仓库现有 lint、类型、schema、原生提示核心和 Jest 门禁。

- 运行定向完成页、协调器、仓储、本地化和无障碍测试。
- 运行 `npm run lint`、`npm run typecheck` 和
  `npm test -- --runInBand --no-watchman`；最终合入前运行 `npm run check`。
- 验证免费、Premium、满仓、离线、首次完成与重玩不会错误发放或重复显示补给。

交付：定向测试和完整仓库门禁通过。

完成条件：`npm run check` 通过；若存在可复现的既有失败，必须记录证据，不能直接忽略。

### 任务 9：完成设备验收和文档收口

主要文件：`docs/game-feature-plan.md`、本文件，以及必要的简洁设备验收记录。

- iOS 与 Android 分别验证浅色、深色、简体中文、德语长文案、最大动态字体和减少动态效果。
- 用 VoiceOver 与 TalkBack 完成：进入结果页、读取成绩、打开/关闭难度框、直接开始其他难度、返回
  完成页复盘和返回首页。
- 保存必要的最终截图和简洁验收摘要，不保留临时实验数据库或重复审计档案。
- 在实现完成后更新 `docs/game-feature-plan.md` 的完成页显示和交互事实。
- 若结算 DTO 或最佳时间口径改变，更新相关架构和奖励验收文档。
- 只记录当前开发基线，不为本次首发前改版建立新的内容版本、协议版本或历史兼容分支。

交付：双端关键路径通过，本文件状态更新为“已完成”，权威文档与实际 Release 行为一致。

完成条件：第 14 节测试矩阵全部通过，第 15 节完成定义全部满足。

## 14. 关键测试矩阵

| 场景 | 预期 |
| --- | --- |
| 零失误且未用提示 | 完美标题或更高优先级标题；不堆叠重复事实标签 |
| 有失误但未用提示 | 独立完成反馈；语气不责备 |
| 使用智能提示 | 提示辅助反馈；不使用“独立”“完全靠自己”等错误表扬 |
| 首次完成且刷新纪录 | 新纪录为主标题；不额外堆叠标签 |
| 免费用户首次完成 | 有勋章和短标题；不显示道具领取层 |
| Premium 首次完成 | 显示两个道具图示、本局应得数量和“收下”按钮 |
| Premium 满仓 | 与普通奖励使用相同领取表现，不显示库存概念 |
| 重复完成 | 可显示新纪录与完成荣誉；不再次显示首次补给 |
| 打开后取消难度框 | 保持完成页内容、滚动位置和复盘入口 |
| 选择其他难度 | 直接进入所选难度新题，不经过首页 |
| 从复盘返回 | 回到同一完成页；文案不变；完整庆祝不重复播放 |
| 减少动态效果 | 静态奖杯立即显示；操作能力完全保留 |
| 最大动态字体 | 短标题、四项统计和按钮保持可读，可滚动 |
| 屏幕阅读器 | 标题、数据、奖励图示、领取按钮和选择框按语义顺序可操作 |

## 15. 完成定义

满足以下条件才可标记本次改进完成：

- 每种合法完成类型都有事实准确、简短且有游戏感的标题与勋章反馈。
- “进度和奖励已安全保存”已从四语正式界面删除。
- 免费、Premium、首次完成和重玩的领取资格与领域结算一致；完成页不呈现库存状态。
- “换个难度”在完成页直接打开选择框，取消不离页，选择后直接开局。
- 快速铅笔使用次数与其他三项统计保持一行；完成类型和新最佳状态由主标题按优先级呈现。
- 首页难度选择和失败页没有行为回归。
- 四语、双主题、动态字体、减少动态效果、VoiceOver 与 TalkBack 关键路径通过。
- 所有自动化门禁通过，且未新增奖励经济、广告入口、成长入口或持久化成就系统。
